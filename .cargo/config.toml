# Use mingw64 gcc as the linker for the GNU Windows target.
# lld is not needed since we removed cdylib (desktop-only build uses rlib).
[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
